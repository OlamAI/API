syntax = "proto3";
package pb;

message SpawnAgentMessage {
  int32 X = 1;
  int32 Y = 2;
}

message SpawnAgentResultMessage {
  string Status = 1;
}

service Simulation {
  // Execute something on data
  rpc SpawnAgent(SpawnAgentMessage) returns (SpawnAgentResultMessage) {}
}
